apiVersion: apps/v1
kind: Deployment
metadata:
  name: member-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: member-service
  template:
    metadata:
      labels:
        app: member-service
    spec:
      containers:
        - name: member-service
          image: member-service:latest
          ports:
            - containerPort: 8087
          env:
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: mongo-uri
                  key: MONGO_URI
            - name: AUTH0_ISSUER
              valueFrom:
                secretKeyRef:
                  name: auth0-issuer
                  key: AUTH0_ISSUER
            - name: JWT_SET_URI
              valueFrom:
                secretKeyRef:
                  name: jwt-set-uri
                  key: JWT_SET_URI
            - name: GOOGLE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: google-api-key
                  key: GOOGLE_API_KEY
---
apiVersion: v1
kind: Service
metadata:
  name: member-service-srv
spec:
  type: ClusterIP
  selector:
    app: member-service
  ports:
    - name: member-service
      protocol: TCP
      port: 8087
      targetPort: 8087