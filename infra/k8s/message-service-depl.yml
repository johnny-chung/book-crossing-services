apiVersion: apps/v1
kind: Deployment
metadata:
  name: message-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: message-service
  template:
    metadata:
      labels:
        app: message-service
    spec:
      containers:
        - name: message-service
          image: goodmanisltd/bookcrossing-message-service:latest
          ports:
            - containerPort: 8089
          env:
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: mongo-uri
                  key: MONGO_URI
            - name: AUTH0_ISSUER
              valueFrom:
                secretKeyRef:
                  name: auth0-issuer
                  key: AUTH0_ISSUER
            - name: JWT_SET_URI
              valueFrom:
                secretKeyRef:
                  name: jwt-set-uri
                  key: JWT_SET_URI
            - name: GOOGLE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: google-api-key
                  key: GOOGLE_API_KEY
---
apiVersion: v1
kind: Service
metadata:
  name: message-service-srv
spec:
  type: ClusterIP
  selector:
    app: message-service
  ports:
    - name: message-service
      protocol: TCP
      port: 8089
      targetPort: 8089